This folder requires some investigation.
Seems like the important file to load test data to the database is:
- loadMaster
  and the files it calls.

However, other files were pushed to git as well in case needed because we don't have the time right now to evalute their usage:
- load_data.sql
